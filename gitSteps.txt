Setp 1
 git config --global user.name <Any User Name>
 git config --global user.name Saleel

 git config --global user.email <git login emailID>
 git config --global user.email saleel.github@gmail.com

Step 2
 git init <creates .git folders in local folder>

Step 3
 git status [the git status command to see which files git knows exist.]

Step 4
  git add <filename || --All> command [To add a file to a commit, you first need to add it to the staging environment. To do this, you can use the git add <filename> command ]

Step 5
 git commit -m "This is my first commit!"

Step 6
 git remote add origin https://github.com/saleelgithub/Test1.git

Step 7
 git push -u origin master




